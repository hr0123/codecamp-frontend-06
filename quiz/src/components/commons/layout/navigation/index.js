import styled from "@emotion/styled";

const Navigation = styled.div`
  background-color: orange;
  height: 50px;
`;

export default function QuizLayoutNavigation() {
  return <Navigation>네비게이션 영역</Navigation>;
}
